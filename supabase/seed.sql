-- Scoutd seed data (run after migration, requires auth users to exist)
-- For local dev, create test users via Supabase Auth UI first, then update UUIDs below.

-- Example: feature flags for demo
-- INSERT INTO featured_entities (entity_type, entity_id, sort_order) VALUES ('player', '<uuid>', 1);
